export enum ROLE {
    STAFF = "staff",
    GUARD = "guard",
    ADMIN = "admin",
}

export enum ParkingSpaceState {
    OCCUPIED = "occupied",
    AVAILABLE = "available",
    ERROR = "error",
}

export enum AUTHACTION {
    LOGIN = "LOGIN",
    LOGOUT = "LOGOUT",
    UNEXPIRED = "UNEXPIRED",
}
